apiVersion: v1
kind: Service
metadata:
  name: pingpong-svc
spec:
  type: ClusterIP
  selector:
    app: pingpong
  ports:
    - port: 2300
      name: external
      protocol: TCP
      targetPort: 3000
    - port: 80
      name: internal
      protocol: TCP
      targetPort: 3000