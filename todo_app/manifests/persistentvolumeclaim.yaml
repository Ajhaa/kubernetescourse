apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-claim
  namespace: todoapp-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
