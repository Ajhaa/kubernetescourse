apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: uuid-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: uuid-svc
          servicePort: 2300